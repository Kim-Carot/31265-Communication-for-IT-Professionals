
  # Community Preparedness Display UI

  This is a code bundle for Community Preparedness Display UI. The original project is available at https://www.figma.com/design/ntlNuh2olQ118XZ9s0kl9D/Community-Preparedness-Display-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  